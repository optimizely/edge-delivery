{
  "name": "edge-delivery-from-kv",
  "license": "Apache-2.0",
  "version": "0.0.0",
  "scripts": {
    "deploy": "wrangler deploy --env prod",
    "dev": "wrangler dev --env dev",
    "start": "wrangler dev --env dev",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@optimizely/edge-delivery": "^1.0.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.96.0"
  }
}
